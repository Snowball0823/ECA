# Checkpoints

Place pretrained model weights here. The default config layout is:

```text
checkpoints/LLaVA/
checkpoints/InternVL/
```

Large checkpoint files are ignored by git. See the main README for download commands.
