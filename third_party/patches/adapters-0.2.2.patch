--- /tmp/adapters_pypi/upstream/adapters/configuration/adapter_fusion_config.py	2026-06-04 08:21:45.172808328 +0000
+++ third_party/adapters-0.2.2/src/adapters/configuration/adapter_fusion_config.py	2026-01-19 10:56:30.132631541 +0000
@@ -19,6 +19,8 @@
     value_before_softmax: bool
     value_initialized: str
     dropout_prob: float
+    fusion_type: str = "bert"
+    gate_reduction_factor: int = 16
 
     @classmethod
     def load(cls, config: Union[dict, str], **kwargs):
@@ -60,6 +62,7 @@
     value_before_softmax: bool = True
     value_initialized: str = False
     dropout_prob: float = None
+    gate_reduction_factor: int = 16
 
 
 @dataclass(eq=False)
@@ -78,8 +81,33 @@
     value_before_softmax: bool = True
     value_initialized: str = True
     dropout_prob: float = None
+    gate_reduction_factor: int = 16
 
 
-ADAPTERFUSION_CONFIG_MAP = {"static": StaticAdapterFusionConfig(), "dynamic": DynamicAdapterFusionConfig()}
+@dataclass(eq=False)
+class LinearAdapterFusionConfig(AdapterFusionConfig):
+    """
+    Linear gated fusion that avoids computing attention-style dot products.
+    """
+
+    fusion_type: str = "linear"
+    key: bool = False
+    query: bool = True
+    value: bool = True
+    query_before_ln: bool = False
+    regularization: bool = False
+    residual_before: bool = False
+    temperature: bool = False
+    value_before_softmax: bool = True
+    value_initialized: str = True
+    dropout_prob: float = None
+    gate_reduction_factor: int = 16
+
+
+ADAPTERFUSION_CONFIG_MAP = {
+    "static": StaticAdapterFusionConfig(),
+    "dynamic": DynamicAdapterFusionConfig(),
+    "linear": LinearAdapterFusionConfig(),
+}
 
 DEFAULT_ADAPTERFUSION_CONFIG = "dynamic"
--- /tmp/adapters_pypi/upstream/adapters/methods/modeling.py	2026-06-04 08:21:45.176808459 +0000
+++ third_party/adapters-0.2.2/src/adapters/methods/modeling.py	2026-01-19 10:56:54.817443182 +0000
@@ -362,7 +362,6 @@
         self.reduction = self.T / 1000.0
 
     def forward(self, query, key, value, residual, output_attentions: bool = False):
-
         if self.config["residual_before"]:
             value += residual[:, :, None, :].repeat(1, 1, value.size(2), 1)
 
@@ -409,6 +408,56 @@
             return context_layer
 
 
+class LinearAdapterFusion(nn.Module):
+    """
+    Lightweight adapter fusion using a small gating MLP instead of attention.
+    """
+
+    def __init__(
+        self,
+        config: AdapterFusionConfig,
+        dense_size,
+        attention_probs_dropout_prob,
+        num_adapters: int,
+    ):
+        super().__init__()
+        self.config = config
+        self.dense_size = dense_size
+        self.num_adapters = num_adapters
+        self.dropout = nn.Dropout(attention_probs_dropout_prob)
+        reduction = max(1, dense_size // max(1, config.gate_reduction_factor))
+        self.gate_down = nn.Linear(dense_size, reduction)
+        self.gate_act = nn.SiLU()
+        self.gate_up = nn.Linear(reduction, num_adapters)
+        if self.config["value"]:
+            self.value = nn.Linear(self.dense_size, self.dense_size, bias=False)
+            self.value.apply(Adapter.init_bert_weights)
+            if self.config["value_initialized"]:
+                self.value.weight.data = (torch.zeros(self.dense_size, self.dense_size) + 0.000001).fill_diagonal_(1.0)
+
+    def forward(self, query, key, value, residual, output_attentions: bool = False):
+        gate_hidden = self.gate_act(self.gate_down(self.dropout(query)))
+        gate_logits = self.gate_up(gate_hidden)
+        gate = torch.softmax(gate_logits, dim=-1)
+
+        if self.config["value"] and self.config["value_before_softmax"]:
+            value_layer = self.value(value)
+        else:
+            value_layer = value
+
+        context_layer = torch.sum(value_layer * gate.unsqueeze(-1), dim=2)
+
+        if self.config["value"] and not self.config["value_before_softmax"]:
+            context_layer = self.value(context_layer)
+
+        if not self.config["residual_before"]:
+            context_layer += residual
+
+        if output_attentions:
+            return context_layer, gate.detach().cpu().numpy()
+        return context_layer
+
+
 # Invertible Adapters
 
 
--- /tmp/adapters_pypi/upstream/adapters/methods/bottleneck.py	2026-06-04 08:21:45.176808459 +0000
+++ third_party/adapters-0.2.2/src/adapters/methods/bottleneck.py	2026-01-19 10:56:37.872886042 +0000
@@ -16,7 +16,7 @@
 from ..configuration import BnConfig
 from ..context import ForwardContext
 from .adapter_layer_base import ComposableAdapterLayerBase
-from .modeling import Adapter, BertFusion, ParallelAdapter
+from .modeling import Adapter, BertFusion, LinearAdapterFusion, ParallelAdapter
 
 
 class BottleneckState(NamedTuple):
@@ -126,11 +126,20 @@
         if self.adapters_config.common_config_value(adapter_names, self.location_key):
             fusion_config = self.adapters_config.get_fusion(adapter_names)
             dropout_prob = fusion_config.dropout_prob or getattr(self.model_config, "attention_probs_dropout_prob", 0)
-            fusion = BertFusion(
-                fusion_config,
-                self.model_config.hidden_size,
-                dropout_prob,
-            )
+            fusion_type = getattr(fusion_config, "fusion_type", "bert")
+            if fusion_type == "linear":
+                fusion = LinearAdapterFusion(
+                    fusion_config,
+                    self.model_config.hidden_size,
+                    dropout_prob,
+                    num_adapters=len(adapter_names),
+                )
+            else:
+                fusion = BertFusion(
+                    fusion_config,
+                    self.model_config.hidden_size,
+                    dropout_prob,
+                )
             fusion.train(self.training)  # make sure training mode is consistent
             self.adapter_fusion_layer[",".join(adapter_names)] = fusion
 
@@ -255,7 +264,6 @@
         Performs adapter fusion with the given adapters for the given input.
         """
         context = ForwardContext.get_context()
-
         # config of _last_ fused adapter is significant
         fusion_config = self.adapters_config.get_fusion(adapter_setup.name)
         last = adapter_setup.last()
--- /tmp/adapters_pypi/upstream/adapters/methods/adapter_layer_base.py	2026-06-04 08:21:45.176808459 +0000
+++ third_party/adapters-0.2.2/src/adapters/methods/adapter_layer_base.py	2025-02-11 08:25:25.644272592 +0000
@@ -305,9 +305,11 @@
         """
         for i, adapter_stack_layer in enumerate(adapter_setup):
             if isinstance(adapter_stack_layer, AdapterCompositionBlock):
-                self.check_composition_valid(adapter_setup, adapter_stack_layer, lvl)
-                composition_func = self._get_compose_func(type(adapter_stack_layer))
-                state = composition_func(adapter_stack_layer, state, lvl=lvl + 1)
+                # check sublayers
+                if all(sub_layer in self.adapter_modules for sub_layer in adapter_stack_layer):
+                    self.check_composition_valid(adapter_setup, adapter_stack_layer, lvl)
+                    composition_func = self._get_compose_func(type(adapter_stack_layer))
+                    state = composition_func(adapter_stack_layer, state, lvl=lvl + 1)
             elif adapter_stack_layer in self.adapter_modules:
                 state = self.pre_block(adapter_stack_layer, state)
                 state = self.compose_single(adapter_stack_layer, state, lvl=lvl + 1)
@@ -444,7 +446,6 @@
         """
 
         state = self.pre_block(adapter_setup, state)
-
         children_states = []
         for i, child in enumerate(adapter_setup):
             if isinstance(child, AdapterCompositionBlock):
